# Copy to `.env.local` (never commit `.env.local`). See project dev guide.
# VITE_SITE_ID must match the GitHub Actions variable SITE_ID for pull-config / submissions.
VITE_SITE_ID=giannis-karagiannidis

# Base URL of the HAYC contact API (no trailing slash). Required for contact forms to submit.
VITE_API_URL=
